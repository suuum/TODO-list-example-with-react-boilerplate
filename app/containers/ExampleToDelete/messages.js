/*
 * ExampleToDelete Messages
 *
 * This contains all the text for the ExampleToDelete container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExampleToDelete';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExampleToDelete container!',
  },
});
